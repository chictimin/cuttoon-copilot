@AGENTS.md

# CLAUDE.md

컷툰 제작 AI 코파일럿 — 리브라이블리 기업 연계 해커톤 프로젝트(실 개발 14일: 1차 5일 + 2차 10일). 스택·폴더 소유권·브랜치·git 규칙은 `README.md` 참조 — 여기서는 중복 기재하지 않는다.

## 절대 원칙 3가지

이 프로젝트의 모든 기능 판단은 아래 세 전제에서 나온다. 새 기능을 제안·구현하기 전에 이 셋에 위배되는지 먼저 확인한다.

1. **API 크레딧 미제공 가정** — 이미지 생성 호출 수를 설계 단계에서 최소화한다. "일단 다 만들고 나중에 줄이자"는 이 전제와 충돌한다.
2. **"딸깍" UX 최상위** — 사용자는 비전문가(디자이너·개발자 아님)다. 조작 단계를 늘리는 설계는 전부 기각. 타이핑을 강요하는 화면 금지 — 모든 흐름은 선택지 클릭 + "알아서 해줘"로 어느 지점에서든 이탈 가능해야 한다.
3. **협업 기능 제외** — 워크스페이스·초대코드·권한 3단계·동시 편집·코멘트 없음. 단일 사용자 전제.

세 전제가 동시에 걸리면 선택지가 좁아진다: 크레딧이 없으니 생성을 줄여야 하는데, 딸깍이라 사용자에게 그 부담을 떠넘길 수도 없다. 아래 결정들은 전부 "제품 내부에서 조용히 줄이는" 시도다.

## 협상 불가 고정값

캐릭터 일관성 방어 수단이 "캐릭터 시트를 매 컷에 reference로 주입" 하나뿐이라 아래는 되돌리지 않는다.

| 값 | 이유 |
|---|---|
| 4컷 고정 | 컷이 늘수록 캐릭터가 컷마다 다른 사람처럼 무너진다. 8·12컷 선택지를 화면에 노출하지 않는다 |
| 등장 캐릭터 1~2명 | 프리셋 단계에서 강제. 인물이 늘면 일관성 유지 난이도가 급증한다 |
| 배경 단순화 | 프리셋에 배경 규칙 포함. 배경이 복잡할수록 컷마다 결과가 튄다 |

## 구현 금지 / 이미 걷어낸 기능

아래는 검토를 거쳐 명시적으로 제외됐다. 관련 요청이 오면 먼저 이 표를 근거로 확인한다.

| 기능 | 왜 제외했나 | 대신 |
|---|---|---|
| 특징 카드 체크박스 선택·누적 | 체크박스 여러 개 앞에서 사용자가 멈춘다 | 자동 확정 + "다시 뽑기" 버튼 |
| 규칙 자연어 입력창 | 빈 입력창은 진입 장벽 | 고급 설정 뒤로 숨김 |
| 규칙 우선순위 조정 | 바꿔도 결과 차이를 시연으로 보여줄 수 없다 | on/off 토글로 대체 |
| 컷 수 질문 | 묻는 것 자체가 마찰 | 4컷 기본값, 변경은 옵션 |
| 콘티 텍스트 3안 (rev.1 축소안, 철회됨) | 텍스트를 읽혀야 함 = 딸깍 실패 | 표지컷 이미지 3안 |
| 버전 비교(diff) UI | 목록을 열어보지 않는다 | 최신만 표시 + 되돌리기 버튼 1개 |
| 워크스페이스 · 초대코드 · 권한 3단계 | 협업 제외로 존재 이유 소멸, 로그인부터 마찰 | 계층 삭제 (`Project → Session → Output`) |
| 검수자 코멘트 | 협업 제외에 포함 | — |
| 멀티 프로바이더 선택 UI | 크레딧 미확정 + 화면 마찰 | 단일 프로바이더 하드코딩. 어댑터 인터페이스만 유지 |
| 로컬 이미지 생성(ComfyUI 등) | 인프라 세팅만으로 수일 소요, 해커톤 범위 밖 | 장기 운영 비용 절감 방안으로 발표자료에만 기재 |
| 실제 인물 캐릭터화 | 초상권 미확인 | 의뢰사 답변 수령 전까지 손대지 않는다 |
| 연재 확장 ("2편 만들기") | 1편 품질도 미확정인데 컨텍스트 유지가 이중으로 걸림 | 후순위(P4 버퍼), 발표에서는 로드맵으로만 언급 |
| 애셋 조립(레이아웃 합성) 방식 | 일관성 100%·호출 0회지만, 사용자가 조립 결과를 확인·수정해야 해 딸깍과 충돌 | 캐릭터 시트 reference 주입 + 텍스트 레이어 분리 렌더링으로 대체. 내부 구현 아이디어(레이어 분리·reference 주입)만 계승 |

## 데이터 계약 (`spec/`)

- **포맷 소유권은 A①**, 값 채우기는 B. B가 슬롯 추가를 제안할 수는 있으나 확정은 A①. `spec/` 스키마 변경은 A① 승인 필요.
- `preset.schema.json` / `storyboard.schema.json` 필드는 **enum 고정 슬롯만** — 자유 서술 금지. `expression: "아련함"`처럼 창작된 값은 이미지 모델이 못 알아듣는다.
- **금지**: 스키마에 `domain` 류 필드 추가. 도메인 개념이 스키마에 들어가면 범용화가 오히려 어려워진다(아래 "범용화 여지" 참조).
- `preset_version` / `storyboard_version` 필드는 항상 유지 — 스키마 변경에 대비.
- 최종 프롬프트 문자열 조립은 **B 소유**. A는 구조화된 필드까지만 넘긴다.
- `spec/vocabulary.json`은 B의 P0 스파이크 산출물이 확정본이다. 확정 전까지는 `vocabulary_version: "0-provisional"` 임시본만 사용하고, A는 그 안에서만 값을 고른다.

### 인터페이스 계약 5개

| # | 방향 | 페이로드 |
|---|---|---|
| ① | A → B | 레퍼런스 이미지 |
| ② | B → A | 추출된 스타일 값 |
| ③ | A → B | 확정 Preset JSON + Storyboard JSON (`variants[3]` 포함) |
| ④ | B → A | 생성 이미지 애셋 + 메타(`width`/`height`/`reserved_zone`) |
| ⑤ | B → A | 허용 어휘 사전 (`vocabulary.json`) |

## 아키텍처 결정

- **브레인스토밍 = 슬롯 채우기이지 대화 기록이 아니다.** 부분 채워진 Storyboard JSON(draft) 위에 얹힌 얇은 층으로 구현한다. 최대 3턴, 매 턴 선택지 3개 + "직접 쓸게" + "알아서 해줘". 소재에 이미 정보가 있으면 해당 턴은 건너뛴다. 종료 판정은 "필수 슬롯이 전부 찼는가"로 결정적이어야 한다.
- **표지컷만 3안, 나머지 3컷은 선택 후 생성한다.** 4컷×3안(12장)이 아니라 표지 3장 + 나머지 3장 = 6장. 표지 3안은 **독립 호출**(세션 누적 없음 — 누적하면 2안이 1안에 끌려가 서로 비슷해진다), 나머지 3컷은 **`ai.chats.create()`로 누적**해 생성한다. 이 순서를 바꾸지 않는다.
- **대사는 텍스트 레이어로 얹는다. 이미지에 굽지 않는다.** 오타 수정이 재생성 없이 즉시 반영되어야 딸깍이 유지된다. 말풍선 자리는 이미지 생성 시점에 `reserved_zone`으로 미리 비워둔다(자리를 나중에 찾는 것보다 쉬움).
- **말풍선 드래그 보정은 위치(`offset`)만 허용한다.** 크기 손잡이·꼬리 방향·폰트는 열지 않는다 — 열면 편집기가 되어 딸깍이 흐려진다. 크기는 글자 수로, 꼬리는 화자 방향으로 자동 결정한다.
- **세션 내부와 세션 사이를 다르게 취급한다.** 세션 내부는 `chats` 히스토리를 그대로 유지(4컷 일관성 목적). 세션 사이는 히스토리를 통째로 넘기지 않고 채택 이미지 + 맥락 텍스트로 압축한다 — 그렇지 않으면 인풋 토큰 한도(65,536)에 부딪힌다.
- **`ImageProvider` 어댑터(`lib/gemini/provider.ts`)에 `chatSession`을 노출하지 않는다.** 나노바나나의 누적 세션 개념을 인터페이스에 그대로 드러내면 ComfyUI 등 다른 프로바이더로의 이관이 불가능해진다.
- **DB에는 저장하되 기능은 만들지 않는 컬럼 3개**: `sessions.chat_history`(컷 재생성용) · `selections`(승격 규칙용, 로직은 보류) · `presets.reference_asset_ids`(누적 메모리용, 지금은 빈 배열). 승격 판정 로직 자체는 만들지 않는다 — 로직 없이도 위 저장 규칙만 지키면 나중에 읽는 코드만 붙이면 된다.

## 범용화 여지 (내부 전략 — 발표에서는 언급하지 않는다)

대외적으로는 리브라이블리 케이스로만 시연하지만, 내부적으로는 범용 마케팅 인스타 컷툰 도구로 확장 가능하게 짠다. 도메인에 묶인 곳은 딱 두 곳뿐이다.

| 업종에 묶인 곳 | 어떻게 푸나 |
|---|---|
| 서사 템플릿 (공감형·정보형·사례형) | 코드가 아니라 JSON 데이터 파일로 분리 |
| 마지막 컷 CTA 목록 | 동일하게 데이터 파일로 분리 |

나머지(프리셋 구조·대화 3턴·표지컷 3안·컷 에디터·Export)는 이미 도메인 무관이므로 손댈 필요 없다.

## 일정 게이트 (P0~P4, 실 개발 14일)

| 구간 | 목표 | 통과 조건 |
|---|---|---|
| P0 | 계약 확정 + 기술 스파이크 | **캐릭터 4컷 동일성 육안 통과** / **말풍선·글자 생성 억제 가능** — 둘 중 하나라도 No면 P1 진입 전 범위를 추가로 축소한다 |
| P1 | 골든 패스 뼈대 | 레퍼런스 업로드 → 프리셋 자동 확정 → 저장 → 캐릭터 시트 화면 표시 |
| P2 | 작업 루프 완성 | 소재 입력 → 4컷 완성 → 대사 수정 → v2 저장이 끊김 없이 |
| P3 | 게시 패키지 + 아카이브 | 완성본이 ZIP으로 실제 다운로드 |
| P4 | 통합 · 발표 | 골든 패스 2분 리허설 |

P0의 두 게이트가 이 프로젝트에서 가장 중요한 판단점이다 — 실패 시 이후 계획의 전제 자체가 바뀐다(예: 말풍선 억제 실패 → 텍스트 레이어 방식 재검토 필요).

## 참고 문서

- 데이터 계약 상세: vault `study/02-해커톤 프로젝트/컷툰 AI 코파일럿 — 스키마·계약 v0.md`
- 팀 분할·설계 결정 근거: vault `study/02-해커톤 프로젝트/컷툰 AI 코파일럿 — 팀 분할 및 구현 페이즈.md`
- 완성도·구현가능성 리스크 분석: vault `study/02-해커톤 프로젝트/컷툰 AI 코파일럿 — 완성도·구현가능성 리스크 필터링.md`
- 요구사항 원본(v0.6): vault `study/02-해커톤 프로젝트/컷툰 제작 AI 코파일럿 요구사항 정리.md`
- 스택·폴더 소유권·브랜치·git 규칙: `README.md`
